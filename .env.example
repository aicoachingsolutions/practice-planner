NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Fallback when Origin/Host headers are missing. Use the same host:port you open in the browser.
# Supabase Dashboard → Auth → URL configuration: add Redirect URLs for each you use, e.g.
# http://localhost:3000/auth/callback and http://127.0.0.1:3000/auth/callback
NEXT_PUBLIC_SITE_URL=http://localhost:3000
OPENAI_API_KEY=
# Optional. Defaults to gpt-4o-mini.
OPENAI_MODEL=
